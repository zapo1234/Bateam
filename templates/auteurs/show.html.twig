{% extends 'base.html.twig' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <h1>Auteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ auteur.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ auteur.lastname }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ auteur.age }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ auteur.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('auteurs_index') }}">back to list</a>

    <a href="{{ path('auteurs_edit', {'id': auteur.id}) }}">edit</a>

    {{ include('auteurs/_delete_form.html.twig') }}
{% endblock %}
